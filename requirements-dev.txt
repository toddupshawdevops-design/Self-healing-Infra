# Development & testing only — not deployed to Lambda
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
moto[dynamodb,ec2,autoscaling,rds,s3,sns,ssm]>=5.0.0
responses>=0.25.0
ruff>=0.3.0
mypy>=1.8.0
boto3-stubs[ec2,autoscaling,rds,ssm,sns,s3,cloudwatch,dynamodb,lambda]>=1.34.0
